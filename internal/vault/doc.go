// Package vault holds the domain logic for vault items — the application-level
// rules around encrypted items and the protected vault key — sitting between the
// API handlers and the storage layer.
//
// Fleshed out alongside Phases 2–3.
package vault
